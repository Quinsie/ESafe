<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:context="http://www.springframework.org/schema/context"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="
                http://www.springframework.org/schema/security
                http://www.springframework.org/schema/security/spring-security.xsd
                http://www.springframework.org/schema/beans
                http://www.springframework.org/schema/beans/spring-beans.xsd
                http://www.springframework.org/schema/context
                http://www.springframework.org/schema/context/spring-context.xsd">

    <context:property-placeholder
        ignore-resource-not-found="false"
        system-properties-mode="OVERRIDE"
        location="classpath:egovframework/spring/risk-security.properties"/>

    <http use-expressions="true" auto-config="true">
        <csrf/>
        <intercept-url pattern="/resources/**" access="permitAll"/>
        <intercept-url pattern="/login.do" access="permitAll"/>
        <intercept-url pattern="/perform_login.do" access="permitAll"/>
        <intercept-url pattern="/accessDenied.do" access="permitAll"/>

        <intercept-url pattern="/refreshWeatherData.do" access="hasRole('ADMIN')"/>
        <intercept-url pattern="/admin/**" access="hasRole('ADMIN')"/>

        <intercept-url pattern="/downloadDangerBuildingExcel.do" access="hasAnyRole('USER','ADMIN')"/>
        <intercept-url pattern="/risk*.do" access="hasAnyRole('USER','ADMIN')"/>
        <intercept-url pattern="/select*.do" access="hasAnyRole('USER','ADMIN')"/>

        <form-login login-page="/login.do"
                    login-processing-url="/perform_login.do"
                    default-target-url="/riskDashboard.do"
                    authentication-failure-url="/login.do?error=Y"
                    username-parameter="username"
                    password-parameter="password"/>
        <logout logout-url="/logout.do" logout-success-url="/login.do?logout=Y"/>
        <access-denied-handler error-page="/accessDenied.do"/>
    </http>

    <authentication-manager>
        <authentication-provider>
            <user-service>
                <user name="${risk.security.admin.username}"
                      password="${risk.security.admin.password}"
                      authorities="ROLE_ADMIN,ROLE_USER"/>
                <user name="${risk.security.user.username}"
                      password="${risk.security.user.password}"
                      authorities="ROLE_USER"/>
            </user-service>
        </authentication-provider>
    </authentication-manager>

</beans:beans>
