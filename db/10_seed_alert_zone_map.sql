-- Seed data for TB_ALERT_ZONE_MAP (synced from H2 09_seed_alert_zone_map.sql)
-- Generated automatically.

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1010100' AS ZONE_CD, '서울' AS ZONE_NM, '서울특별시' AS PARENT_REGION, '서울특별시 전역' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1010200' AS ZONE_CD, '광명' AS ZONE_NM, '경기도' AS PARENT_REGION, '광명시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1010300' AS ZONE_CD, '과천' AS ZONE_NM, '경기도' AS PARENT_REGION, '과천시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1010400' AS ZONE_CD, '안산' AS ZONE_NM, '경기도' AS PARENT_REGION, '안산시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1010500' AS ZONE_CD, '시흥' AS ZONE_NM, '경기도' AS PARENT_REGION, '시흥시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1010600' AS ZONE_CD, '부천' AS ZONE_NM, '경기도' AS PARENT_REGION, '부천시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1010700' AS ZONE_CD, '김포' AS ZONE_NM, '경기도' AS PARENT_REGION, '김포시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1010800' AS ZONE_CD, '인천' AS ZONE_NM, '인천광역시' AS PARENT_REGION, '인천광역시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1010900' AS ZONE_CD, '강화' AS ZONE_NM, '인천광역시' AS PARENT_REGION, '강화군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1011100' AS ZONE_CD, '동두천' AS ZONE_NM, '경기도' AS PARENT_REGION, '동두천시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1011200' AS ZONE_CD, '연천' AS ZONE_NM, '경기도' AS PARENT_REGION, '연천군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1011300' AS ZONE_CD, '포천' AS ZONE_NM, '경기도' AS PARENT_REGION, '포천시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1011400' AS ZONE_CD, '가평' AS ZONE_NM, '경기도' AS PARENT_REGION, '가평군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1011500' AS ZONE_CD, '고양' AS ZONE_NM, '경기도' AS PARENT_REGION, '고양시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1011600' AS ZONE_CD, '양주' AS ZONE_NM, '경기도' AS PARENT_REGION, '양주시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1011700' AS ZONE_CD, '의정부' AS ZONE_NM, '경기도' AS PARENT_REGION, '의정부시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1011800' AS ZONE_CD, '파주' AS ZONE_NM, '경기도' AS PARENT_REGION, '파주시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1011900' AS ZONE_CD, '수원' AS ZONE_NM, '경기도' AS PARENT_REGION, '수원시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1012000' AS ZONE_CD, '성남' AS ZONE_NM, '경기도' AS PARENT_REGION, '성남시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1012100' AS ZONE_CD, '안양' AS ZONE_NM, '경기도' AS PARENT_REGION, '안양시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1012200' AS ZONE_CD, '구리' AS ZONE_NM, '경기도' AS PARENT_REGION, '구리시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1012300' AS ZONE_CD, '남양주' AS ZONE_NM, '경기도' AS PARENT_REGION, '남양주시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1012400' AS ZONE_CD, '오산' AS ZONE_NM, '경기도' AS PARENT_REGION, '오산시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1012500' AS ZONE_CD, '평택' AS ZONE_NM, '경기도' AS PARENT_REGION, '평택시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1012600' AS ZONE_CD, '군포' AS ZONE_NM, '경기도' AS PARENT_REGION, '군포시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1012700' AS ZONE_CD, '의왕' AS ZONE_NM, '경기도' AS PARENT_REGION, '의왕시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1012800' AS ZONE_CD, '하남' AS ZONE_NM, '경기도' AS PARENT_REGION, '하남시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1012900' AS ZONE_CD, '용인' AS ZONE_NM, '경기도' AS PARENT_REGION, '용인시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1013000' AS ZONE_CD, '이천' AS ZONE_NM, '경기도' AS PARENT_REGION, '이천시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1013100' AS ZONE_CD, '안성' AS ZONE_NM, '경기도' AS PARENT_REGION, '안성시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1013200' AS ZONE_CD, '화성' AS ZONE_NM, '경기도' AS PARENT_REGION, '화성시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1013300' AS ZONE_CD, '여주' AS ZONE_NM, '경기도' AS PARENT_REGION, '여주시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1013400' AS ZONE_CD, '광주' AS ZONE_NM, '경기도' AS PARENT_REGION, '광주시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1013500' AS ZONE_CD, '양평' AS ZONE_NM, '경기도' AS PARENT_REGION, '양평군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1013600' AS ZONE_CD, '옹진' AS ZONE_NM, '인천광역시' AS PARENT_REGION, '옹진군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1014100' AS ZONE_CD, '서해5도' AS ZONE_NM, '인천광역시' AS PARENT_REGION, '서해5도' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1020110' AS ZONE_CD, '강릉평지' AS ZONE_NM, '강원도' AS PARENT_REGION, '강릉시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1020210' AS ZONE_CD, '동해평지' AS ZONE_NM, '강원도' AS PARENT_REGION, '동해시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1020300' AS ZONE_CD, '태백' AS ZONE_NM, '강원도' AS PARENT_REGION, '태백시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1020410' AS ZONE_CD, '삼척평지' AS ZONE_NM, '강원도' AS PARENT_REGION, '삼척시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1020510' AS ZONE_CD, '속초평지' AS ZONE_NM, '강원도' AS PARENT_REGION, '속초시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1020610' AS ZONE_CD, '고성평지' AS ZONE_NM, '강원도' AS PARENT_REGION, '고성군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1020710' AS ZONE_CD, '양양평지' AS ZONE_NM, '강원도' AS PARENT_REGION, '양양군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1020800' AS ZONE_CD, '영월' AS ZONE_NM, '강원도' AS PARENT_REGION, '영월군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1020910' AS ZONE_CD, '평창평지' AS ZONE_NM, '강원도' AS PARENT_REGION, '평창군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1021010' AS ZONE_CD, '정선평지' AS ZONE_NM, '강원도' AS PARENT_REGION, '정선군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1021100' AS ZONE_CD, '횡성' AS ZONE_NM, '강원도' AS PARENT_REGION, '횡성군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1021200' AS ZONE_CD, '원주' AS ZONE_NM, '강원도' AS PARENT_REGION, '원주시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1021300' AS ZONE_CD, '철원' AS ZONE_NM, '강원도' AS PARENT_REGION, '철원군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1021400' AS ZONE_CD, '화천' AS ZONE_NM, '강원도' AS PARENT_REGION, '화천군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1021510' AS ZONE_CD, '홍천평지' AS ZONE_NM, '강원도' AS PARENT_REGION, '홍천군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1021600' AS ZONE_CD, '춘천' AS ZONE_NM, '강원도' AS PARENT_REGION, '춘천시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1021710' AS ZONE_CD, '양구평지' AS ZONE_NM, '강원도' AS PARENT_REGION, '양구군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1021810' AS ZONE_CD, '인제평지' AS ZONE_NM, '강원도' AS PARENT_REGION, '인제군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1025020' AS ZONE_CD, '강원북부산지' AS ZONE_NM, '강원도' AS PARENT_REGION, '속초시,인제군,고성군,양양군,양구군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1026020' AS ZONE_CD, '강원중부산지' AS ZONE_NM, '강원도' AS PARENT_REGION, '강릉시,평창군,홍천군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1027020' AS ZONE_CD, '강원남부산지' AS ZONE_NM, '강원도' AS PARENT_REGION, '동해시,삼척시,정선군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1030100' AS ZONE_CD, '대전' AS ZONE_NM, '대전광역시' AS PARENT_REGION, '대전광역시 전역' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1030200' AS ZONE_CD, '천안' AS ZONE_NM, '충청남도' AS PARENT_REGION, '천안시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1030300' AS ZONE_CD, '공주' AS ZONE_NM, '충청남도' AS PARENT_REGION, '공주시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1030400' AS ZONE_CD, '아산' AS ZONE_NM, '충청남도' AS PARENT_REGION, '아산시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1030500' AS ZONE_CD, '논산' AS ZONE_NM, '충청남도' AS PARENT_REGION, '논산시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1030600' AS ZONE_CD, '금산' AS ZONE_NM, '충청남도' AS PARENT_REGION, '금산군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1030800' AS ZONE_CD, '부여' AS ZONE_NM, '충청남도' AS PARENT_REGION, '부여군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1030900' AS ZONE_CD, '청양' AS ZONE_NM, '충청남도' AS PARENT_REGION, '청양군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1031000' AS ZONE_CD, '예산' AS ZONE_NM, '충청남도' AS PARENT_REGION, '예산군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1031100' AS ZONE_CD, '태안' AS ZONE_NM, '충청남도' AS PARENT_REGION, '태안군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1031200' AS ZONE_CD, '당진' AS ZONE_NM, '충청남도' AS PARENT_REGION, '당진시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1031300' AS ZONE_CD, '서산' AS ZONE_NM, '충청남도' AS PARENT_REGION, '서산시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1031400' AS ZONE_CD, '보령' AS ZONE_NM, '충청남도' AS PARENT_REGION, '보령시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1031500' AS ZONE_CD, '서천' AS ZONE_NM, '충청남도' AS PARENT_REGION, '서천군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1031600' AS ZONE_CD, '홍성' AS ZONE_NM, '충청남도' AS PARENT_REGION, '홍성군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1031700' AS ZONE_CD, '계룡' AS ZONE_NM, '충청남도' AS PARENT_REGION, '계룡시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1031800' AS ZONE_CD, '세종' AS ZONE_NM, '세종특별자치시' AS PARENT_REGION, '세종시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1040000' AS ZONE_CD, '충청북도' AS ZONE_NM, '충청북도' AS PARENT_REGION, '충청북도' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1040100' AS ZONE_CD, '청주' AS ZONE_NM, '충청북도' AS PARENT_REGION, '청주시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1040200' AS ZONE_CD, '청원' AS ZONE_NM, '충청북도' AS PARENT_REGION, '청원' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1040300' AS ZONE_CD, '보은' AS ZONE_NM, '충청북도' AS PARENT_REGION, '보은군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1040400' AS ZONE_CD, '괴산' AS ZONE_NM, '충청북도' AS PARENT_REGION, '괴산군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1040600' AS ZONE_CD, '옥천' AS ZONE_NM, '충청북도' AS PARENT_REGION, '옥천군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1040700' AS ZONE_CD, '영동' AS ZONE_NM, '충청북도' AS PARENT_REGION, '영동군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1040800' AS ZONE_CD, '충주' AS ZONE_NM, '충청북도' AS PARENT_REGION, '충주시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1040900' AS ZONE_CD, '제천' AS ZONE_NM, '충청북도' AS PARENT_REGION, '제천시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1041000' AS ZONE_CD, '진천' AS ZONE_NM, '충청북도' AS PARENT_REGION, '진천군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1041100' AS ZONE_CD, '음성' AS ZONE_NM, '충청북도' AS PARENT_REGION, '음성군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1041200' AS ZONE_CD, '단양' AS ZONE_NM, '충청북도' AS PARENT_REGION, '단양군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1041300' AS ZONE_CD, '증평' AS ZONE_NM, '충청북도' AS PARENT_REGION, '증평군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1050100' AS ZONE_CD, '광주' AS ZONE_NM, '전라남도' AS PARENT_REGION, '광주시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1050200' AS ZONE_CD, '나주' AS ZONE_NM, '전라남도' AS PARENT_REGION, '나주시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1050300' AS ZONE_CD, '담양' AS ZONE_NM, '전라남도' AS PARENT_REGION, '담양군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1050400' AS ZONE_CD, '곡성' AS ZONE_NM, '전라남도' AS PARENT_REGION, '곡성군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1050500' AS ZONE_CD, '구례' AS ZONE_NM, '전라남도' AS PARENT_REGION, '구례군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1050600' AS ZONE_CD, '장성' AS ZONE_NM, '전라남도' AS PARENT_REGION, '장성군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1050700' AS ZONE_CD, '화순' AS ZONE_NM, '전라남도' AS PARENT_REGION, '화순군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1050800' AS ZONE_CD, '고흥' AS ZONE_NM, '전라남도' AS PARENT_REGION, '고흥군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1050900' AS ZONE_CD, '보성' AS ZONE_NM, '전라남도' AS PARENT_REGION, '보성군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1051000' AS ZONE_CD, '여수' AS ZONE_NM, '전라남도' AS PARENT_REGION, '여수시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1051100' AS ZONE_CD, '광양' AS ZONE_NM, '전라남도' AS PARENT_REGION, '광양시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1051200' AS ZONE_CD, '순천' AS ZONE_NM, '전라남도' AS PARENT_REGION, '순천시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1051300' AS ZONE_CD, '장흥' AS ZONE_NM, '전라남도' AS PARENT_REGION, '장흥군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1051400' AS ZONE_CD, '강진' AS ZONE_NM, '전라남도' AS PARENT_REGION, '강진군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1051500' AS ZONE_CD, '해남' AS ZONE_NM, '전라남도' AS PARENT_REGION, '해남군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1051600' AS ZONE_CD, '완도' AS ZONE_NM, '전라남도' AS PARENT_REGION, '완도군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1051700' AS ZONE_CD, '영암' AS ZONE_NM, '전라남도' AS PARENT_REGION, '영암군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1051800' AS ZONE_CD, '무안' AS ZONE_NM, '전라남도' AS PARENT_REGION, '무안군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1051900' AS ZONE_CD, '함평' AS ZONE_NM, '전라남도' AS PARENT_REGION, '함평군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1052000' AS ZONE_CD, '영광' AS ZONE_NM, '전라남도' AS PARENT_REGION, '영광군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1052100' AS ZONE_CD, '목포' AS ZONE_NM, '전라남도' AS PARENT_REGION, '목포시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1052200' AS ZONE_CD, '신안(흑산면제외)' AS ZONE_NM, '전라남도' AS PARENT_REGION, '신안(흑산면제외)' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1052300' AS ZONE_CD, '진도' AS ZONE_NM, '전라남도' AS PARENT_REGION, '진도군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1052500' AS ZONE_CD, '흑산도.홍도' AS ZONE_NM, '전라남도' AS PARENT_REGION, '흑산도.홍도' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1052600' AS ZONE_CD, '거문도.초도' AS ZONE_NM, '전라남도' AS PARENT_REGION, '거문도.초도' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1060100' AS ZONE_CD, '고창' AS ZONE_NM, '전라북도' AS PARENT_REGION, '고창군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1060200' AS ZONE_CD, '부안' AS ZONE_NM, '전라북도' AS PARENT_REGION, '부안군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1060300' AS ZONE_CD, '군산' AS ZONE_NM, '전라북도' AS PARENT_REGION, '군산시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1060400' AS ZONE_CD, '김제' AS ZONE_NM, '전라북도' AS PARENT_REGION, '김제시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1060500' AS ZONE_CD, '완주' AS ZONE_NM, '전라북도' AS PARENT_REGION, '완주군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1060600' AS ZONE_CD, '진안' AS ZONE_NM, '전북자치도' AS PARENT_REGION, '진안군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1060700' AS ZONE_CD, '무주' AS ZONE_NM, '전북자치도' AS PARENT_REGION, '무주군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1060800' AS ZONE_CD, '장수' AS ZONE_NM, '전북자치도' AS PARENT_REGION, '장수군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1060900' AS ZONE_CD, '임실' AS ZONE_NM, '전라북도' AS PARENT_REGION, '임실군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1061000' AS ZONE_CD, '순창' AS ZONE_NM, '전라북도' AS PARENT_REGION, '순창군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1061100' AS ZONE_CD, '익산' AS ZONE_NM, '전라북도' AS PARENT_REGION, '익산시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1061200' AS ZONE_CD, '정읍' AS ZONE_NM, '전라북도' AS PARENT_REGION, '정읍시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1061300' AS ZONE_CD, '전주' AS ZONE_NM, '전라북도' AS PARENT_REGION, '전주시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1061400' AS ZONE_CD, '남원' AS ZONE_NM, '전라북도' AS PARENT_REGION, '남원시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1070100' AS ZONE_CD, '대구' AS ZONE_NM, '경상북도' AS PARENT_REGION, '대구' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1070300' AS ZONE_CD, '구미' AS ZONE_NM, '경상북도' AS PARENT_REGION, '구미시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1070400' AS ZONE_CD, '영천' AS ZONE_NM, '경상북도' AS PARENT_REGION, '영천시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1070500' AS ZONE_CD, '경산' AS ZONE_NM, '경상북도' AS PARENT_REGION, '경산시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1070600' AS ZONE_CD, '군위' AS ZONE_NM, '경상북도' AS PARENT_REGION, '군위군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1070700' AS ZONE_CD, '청도' AS ZONE_NM, '경상북도' AS PARENT_REGION, '청도군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1070800' AS ZONE_CD, '고령' AS ZONE_NM, '경상북도' AS PARENT_REGION, '고령군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1070900' AS ZONE_CD, '성주' AS ZONE_NM, '경상북도' AS PARENT_REGION, '성주군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1071000' AS ZONE_CD, '칠곡' AS ZONE_NM, '경상북도' AS PARENT_REGION, '칠곡군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1071100' AS ZONE_CD, '김천' AS ZONE_NM, '경상북도' AS PARENT_REGION, '김천시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1071200' AS ZONE_CD, '상주' AS ZONE_NM, '경상북도' AS PARENT_REGION, '상주시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1071300' AS ZONE_CD, '문경' AS ZONE_NM, '경상북도' AS PARENT_REGION, '문경시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1071400' AS ZONE_CD, '예천' AS ZONE_NM, '경상북도' AS PARENT_REGION, '예천군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1071500' AS ZONE_CD, '안동' AS ZONE_NM, '경상북도' AS PARENT_REGION, '안동시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1071600' AS ZONE_CD, '영주' AS ZONE_NM, '경상북도' AS PARENT_REGION, '영주시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1071700' AS ZONE_CD, '의성' AS ZONE_NM, '경상북도' AS PARENT_REGION, '의성군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1071800' AS ZONE_CD, '청송' AS ZONE_NM, '경상북도' AS PARENT_REGION, '청송군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1071910' AS ZONE_CD, '영양평지' AS ZONE_NM, '경상북도' AS PARENT_REGION, '영양군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1072010' AS ZONE_CD, '봉화평지' AS ZONE_NM, '경상북도' AS PARENT_REGION, '봉화군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1072100' AS ZONE_CD, '울릉도.독도' AS ZONE_NM, '울릉도.독도' AS PARENT_REGION, '울릉도.독도' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1072200' AS ZONE_CD, '영덕' AS ZONE_NM, '경상북도' AS PARENT_REGION, '영덕군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1072310' AS ZONE_CD, '울진평지' AS ZONE_NM, '경상북도' AS PARENT_REGION, '울진군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1072400' AS ZONE_CD, '포항' AS ZONE_NM, '경상북도' AS PARENT_REGION, '포항시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1072500' AS ZONE_CD, '경주' AS ZONE_NM, '경상북도' AS PARENT_REGION, '경주시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1075020' AS ZONE_CD, '경북북동산지' AS ZONE_NM, '경상북도' AS PARENT_REGION, '울진군,봉화군,영양군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1080100' AS ZONE_CD, '부산' AS ZONE_NM, '부산광역시' AS PARENT_REGION, '기장군, 해운대구, 수영구, 남구' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1080300' AS ZONE_CD, '울산' AS ZONE_NM, '울산광역시' AS PARENT_REGION, '울산광역시 전역' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1080500' AS ZONE_CD, '양산' AS ZONE_NM, '경상남도' AS PARENT_REGION, '양산시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1080600' AS ZONE_CD, '창원' AS ZONE_NM, '경상남도' AS PARENT_REGION, '창원시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1080900' AS ZONE_CD, '김해' AS ZONE_NM, '경상남도' AS PARENT_REGION, '김해시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1081000' AS ZONE_CD, '밀양' AS ZONE_NM, '경상남도' AS PARENT_REGION, '밀양시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1081100' AS ZONE_CD, '의령' AS ZONE_NM, '경상남도' AS PARENT_REGION, '의령군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1081200' AS ZONE_CD, '함안' AS ZONE_NM, '경상남도' AS PARENT_REGION, '함안군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1081300' AS ZONE_CD, '창녕' AS ZONE_NM, '경상남도' AS PARENT_REGION, '창녕군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1081400' AS ZONE_CD, '진주' AS ZONE_NM, '경상남도' AS PARENT_REGION, '진주시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1081500' AS ZONE_CD, '하동' AS ZONE_NM, '경상남도' AS PARENT_REGION, '하동군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1081600' AS ZONE_CD, '산청' AS ZONE_NM, '경상남도' AS PARENT_REGION, '산청군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1081700' AS ZONE_CD, '함양' AS ZONE_NM, '경상남도' AS PARENT_REGION, '함양군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1081800' AS ZONE_CD, '거창' AS ZONE_NM, '경상남도' AS PARENT_REGION, '거창군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1081900' AS ZONE_CD, '합천' AS ZONE_NM, '경상남도' AS PARENT_REGION, '합천군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1082000' AS ZONE_CD, '통영' AS ZONE_NM, '경상남도' AS PARENT_REGION, '통영시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1082100' AS ZONE_CD, '사천' AS ZONE_NM, '경상남도' AS PARENT_REGION, '사천시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1082200' AS ZONE_CD, '거제' AS ZONE_NM, '경상남도' AS PARENT_REGION, '거제시' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1082300' AS ZONE_CD, '고성' AS ZONE_NM, '경상남도' AS PARENT_REGION, '고성군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1082400' AS ZONE_CD, '남해' AS ZONE_NM, '경상남도' AS PARENT_REGION, '남해군' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1090500' AS ZONE_CD, '제주도산지' AS ZONE_NM, '제주도' AS PARENT_REGION, '제주도산지' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1090600' AS ZONE_CD, '제주도서부' AS ZONE_NM, '제주도' AS PARENT_REGION, '제주도서부' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1090700' AS ZONE_CD, '제주도북부' AS ZONE_NM, '제주도' AS PARENT_REGION, '제주도북부' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1090800' AS ZONE_CD, '제주도동부' AS ZONE_NM, '제주도' AS PARENT_REGION, '제주도동부' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1090900' AS ZONE_CD, '제주도남부' AS ZONE_NM, '제주도' AS PARENT_REGION, '제주도남부' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'L1091000' AS ZONE_CD, '추자도' AS ZONE_NM, '제주도' AS PARENT_REGION, '추자도' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S1131100' AS ZONE_CD, '울산앞바다' AS ZONE_NM, '동해남부앞바다' AS PARENT_REGION, '울산앞바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S1131200' AS ZONE_CD, '경북남부앞바다' AS ZONE_NM, '동해남부앞바다' AS PARENT_REGION, '경북남부앞바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S1131300' AS ZONE_CD, '경북북부앞바다' AS ZONE_NM, '동해남부앞바다' AS PARENT_REGION, '경북북부앞바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S1132000' AS ZONE_CD, '동해남부먼바다' AS ZONE_NM, '동해남부전해상' AS PARENT_REGION, '동해남부먼바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S1151100' AS ZONE_CD, '강원북부앞바다' AS ZONE_NM, '동해중부앞바다' AS PARENT_REGION, '강원북부앞바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S1151200' AS ZONE_CD, '강원중부앞바다' AS ZONE_NM, '동해중부앞바다' AS PARENT_REGION, '강원중부앞바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S1151300' AS ZONE_CD, '강원남부앞바다' AS ZONE_NM, '동해중부앞바다' AS PARENT_REGION, '강원남부앞바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S1152000' AS ZONE_CD, '동해중부먼바다' AS ZONE_NM, '동해중부전해상' AS PARENT_REGION, '동해중부먼바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S1231100' AS ZONE_CD, '전북북부앞바다' AS ZONE_NM, '서해남부앞바다' AS PARENT_REGION, '전북북부앞바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S1231200' AS ZONE_CD, '전북남부앞바다' AS ZONE_NM, '서해남부앞바다' AS PARENT_REGION, '전북남부앞바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S1231300' AS ZONE_CD, '전남북부서해앞바다' AS ZONE_NM, '서해남부앞바다' AS PARENT_REGION, '전남북부서해앞바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S1231400' AS ZONE_CD, '전남중부서해앞바다' AS ZONE_NM, '서해남부앞바다' AS PARENT_REGION, '전남중부서해앞바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S1231500' AS ZONE_CD, '전남남부서해앞바다' AS ZONE_NM, '서해남부앞바다' AS PARENT_REGION, '전남남부서해앞바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S1232000' AS ZONE_CD, '서해남부먼바다' AS ZONE_NM, '서해남부전해상' AS PARENT_REGION, '서해남부먼바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S1251100' AS ZONE_CD, '인천·경기북부앞바다' AS ZONE_NM, '서해중부앞바다' AS PARENT_REGION, '인천·경기북부앞바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S1251200' AS ZONE_CD, '인천·경기남부앞바다' AS ZONE_NM, '서해중부앞바다' AS PARENT_REGION, '인천·경기남부앞바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S1251300' AS ZONE_CD, '충남북부앞바다' AS ZONE_NM, '서해중부앞바다' AS PARENT_REGION, '충남북부앞바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S1251400' AS ZONE_CD, '충남남부앞바다' AS ZONE_NM, '서해중부앞바다' AS PARENT_REGION, '충남남부앞바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S1252000' AS ZONE_CD, '서해중부먼바다' AS ZONE_NM, '서해중부전해상' AS PARENT_REGION, '서해중부먼바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S1311100' AS ZONE_CD, '부산앞바다' AS ZONE_NM, '남해동부앞바다' AS PARENT_REGION, '부산앞바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S1311200' AS ZONE_CD, '경남서부남해앞바다' AS ZONE_NM, '남해동부앞바다' AS PARENT_REGION, '경남서부남해앞바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S1311300' AS ZONE_CD, '경남중부남해앞바다' AS ZONE_NM, '남해동부앞바다' AS PARENT_REGION, '경남중부남해앞바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S1311400' AS ZONE_CD, '거제시동부앞바다' AS ZONE_NM, '남해동부앞바다' AS PARENT_REGION, '거제시동부앞바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S1312000' AS ZONE_CD, '남해동부먼바다' AS ZONE_NM, '남해동부전해상' AS PARENT_REGION, '남해동부먼바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S1321100' AS ZONE_CD, '전남서부남해앞바다' AS ZONE_NM, '남해서부앞바다' AS PARENT_REGION, '전남서부남해앞바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S1321200' AS ZONE_CD, '전남동부남해앞바다' AS ZONE_NM, '남해서부앞바다' AS PARENT_REGION, '전남동부남해앞바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S1322100' AS ZONE_CD, '남해서부서쪽먼바다' AS ZONE_NM, '남해서부전해상' AS PARENT_REGION, '남해서부서쪽먼바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S1322200' AS ZONE_CD, '남해서부동쪽먼바다' AS ZONE_NM, '남해서부전해상' AS PARENT_REGION, '남해서부동쪽먼바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S1323100' AS ZONE_CD, '제주도북부앞바다' AS ZONE_NM, '제주도앞바다' AS PARENT_REGION, '제주도북부앞바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S1323200' AS ZONE_CD, '제주도동부앞바다' AS ZONE_NM, '제주도앞바다' AS PARENT_REGION, '제주도동부앞바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S1323300' AS ZONE_CD, '제주도남부앞바다' AS ZONE_NM, '제주도앞바다' AS PARENT_REGION, '제주도남부앞바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S1323400' AS ZONE_CD, '제주도서부앞바다' AS ZONE_NM, '제주도앞바다' AS PARENT_REGION, '제주도서부앞바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S1324000' AS ZONE_CD, '제주도남쪽먼바다' AS ZONE_NM, '제주도전해상' AS PARENT_REGION, '제주도남쪽먼바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S2110100' AS ZONE_CD, '경북남부앞바다중 평수구역' AS ZONE_NM, '경북남부앞바다' AS PARENT_REGION, '평수구역' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S2110200' AS ZONE_CD, '울산앞바다중 평수구역' AS ZONE_NM, '울산앞바다' AS PARENT_REGION, '평수구역' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S2120100' AS ZONE_CD, '경북북부앞바다중 연안바다' AS ZONE_NM, '경북북부앞바다' AS PARENT_REGION, '연안바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S2120200' AS ZONE_CD, '경북북부앞바다중 영덕연안바다' AS ZONE_NM, '경북북부앞바다' AS PARENT_REGION, '영덕연안바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S2120300' AS ZONE_CD, '경북남부앞바다중 연안바다' AS ZONE_NM, '경북남부앞바다' AS PARENT_REGION, '연안바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S2120400' AS ZONE_CD, '울산앞바다중 연안바다' AS ZONE_NM, '울산앞바다' AS PARENT_REGION, '연안바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S2120500' AS ZONE_CD, '강원중부앞바다중 연안바다' AS ZONE_NM, '강원중부앞바다' AS PARENT_REGION, '연안바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S2120600' AS ZONE_CD, '강원북부앞바다중 연안바다' AS ZONE_NM, '강원북부앞바다' AS PARENT_REGION, '연안바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S2120700' AS ZONE_CD, '강원남부앞바다중 연안바다' AS ZONE_NM, '강원남부앞바다' AS PARENT_REGION, '연안바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S2120800' AS ZONE_CD, '울릉도울릉읍연안바다' AS ZONE_NM, '동해남부앞바다' AS PARENT_REGION, '울릉도울릉읍연안바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S2120900' AS ZONE_CD, '울릉도서면연안바다' AS ZONE_NM, '동해남부앞바다' AS PARENT_REGION, '울릉도서면연안바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S2210100' AS ZONE_CD, '전북북부앞바다중 평수구역' AS ZONE_NM, '전북북부앞바다' AS PARENT_REGION, '평수구역' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S2210200' AS ZONE_CD, '전북남부앞바다중 평수구역' AS ZONE_NM, '전북남부앞바다' AS PARENT_REGION, '평수구역' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S2210300' AS ZONE_CD, '전남북부서해앞바다중 평수구역' AS ZONE_NM, '전남북부서해앞바다' AS PARENT_REGION, '평수구역' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S2210500' AS ZONE_CD, '전남남부서해앞바다중 평수구역' AS ZONE_NM, '전남남부서해앞바다' AS PARENT_REGION, '평수구역' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S2210600' AS ZONE_CD, '충남북부앞바다중 평수구역' AS ZONE_NM, '충남북부앞바다' AS PARENT_REGION, '평수구역' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S2210700' AS ZONE_CD, '충남남부앞바다중 평수구역' AS ZONE_NM, '충남남부앞바다' AS PARENT_REGION, '평수구역' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S2210800' AS ZONE_CD, '인천·경기북부앞바다중 앞평수구역' AS ZONE_NM, '인천·경기북부앞바다' AS PARENT_REGION, '앞평수구역' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S2210900' AS ZONE_CD, '인천·경기북부앞바다중 먼평수구역' AS ZONE_NM, '인천·경기북부앞바다' AS PARENT_REGION, '먼평수구역' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S2211000' AS ZONE_CD, '인천·경기남부앞바다중 앞평수구역' AS ZONE_NM, '인천·경기남부앞바다' AS PARENT_REGION, '앞평수구역' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S2211100' AS ZONE_CD, '인천·경기남부앞바다중 먼평수구역' AS ZONE_NM, '인천·경기남부앞바다' AS PARENT_REGION, '먼평수구역' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S2211200' AS ZONE_CD, '서해남부먼바다중 조도부근평수구역' AS ZONE_NM, '서해남부먼바다' AS PARENT_REGION, '조도부근평수구역' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S2211300' AS ZONE_CD, '전남중부서해앞바다중 먼평수구역' AS ZONE_NM, '전남중부서해앞바다' AS PARENT_REGION, '먼평수구역' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S2211400' AS ZONE_CD, '전남중부서해앞바다중 앞평수구역' AS ZONE_NM, '전남중부서해앞바다' AS PARENT_REGION, '앞평수구역' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S2211500' AS ZONE_CD, '천수만 평수구역' AS ZONE_NM, '서해중부앞바다' AS PARENT_REGION, '천수만 평수구역' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S2310100' AS ZONE_CD, '부산앞바다중 동부평수구역' AS ZONE_NM, '부산앞바다' AS PARENT_REGION, '동부평수구역' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S2310200' AS ZONE_CD, '부산앞바다중 서부평수구역' AS ZONE_NM, '부산앞바다' AS PARENT_REGION, '서부평수구역' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S2310300' AS ZONE_CD, '경남중부남해앞바다중 평수구역' AS ZONE_NM, '경남중부남해앞바다' AS PARENT_REGION, '평수구역' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S2310400' AS ZONE_CD, '경남서부남해앞바다중 동부평수구역' AS ZONE_NM, '경남서부남해앞바다' AS PARENT_REGION, '동부평수구역' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S2310500' AS ZONE_CD, '경남서부남해앞바다중 서부평수구역' AS ZONE_NM, '경남서부남해앞바다' AS PARENT_REGION, '서부평수구역' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S2310600' AS ZONE_CD, '경남서부남해앞바다중 남부평수구역' AS ZONE_NM, '경남서부남해앞바다' AS PARENT_REGION, '남부평수구역' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S2310700' AS ZONE_CD, '전남서부남해앞바다중 평수구역' AS ZONE_NM, '전남서부남해앞바다' AS PARENT_REGION, '평수구역' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S2310800' AS ZONE_CD, '전남동부남해앞바다중 서부평수구역' AS ZONE_NM, '전남동부남해앞바다' AS PARENT_REGION, '서부평수구역' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S2310900' AS ZONE_CD, '전남동부남해앞바다중 동부평수구역' AS ZONE_NM, '전남동부남해앞바다' AS PARENT_REGION, '동부평수구역' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S2320100' AS ZONE_CD, '부산앞바다중 연안바다' AS ZONE_NM, '부산앞바다' AS PARENT_REGION, '연안바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S2320200' AS ZONE_CD, '거제시동부앞바다중 연안바다' AS ZONE_NM, '거제시동부앞바다' AS PARENT_REGION, '연안바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S2320300' AS ZONE_CD, '경남서부남해앞바다중 남해군연안바다' AS ZONE_NM, '경남서부남해앞바다' AS PARENT_REGION, '남해군연안바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S2320400' AS ZONE_CD, '제주도북부앞바다중 연안바다' AS ZONE_NM, '제주도북부앞바다' AS PARENT_REGION, '연안바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S2320600' AS ZONE_CD, '제주도서부앞바다중 연안바다' AS ZONE_NM, '제주도서부앞바다' AS PARENT_REGION, '연안바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S2320700' AS ZONE_CD, '제주도남부앞바다중 연안바다' AS ZONE_NM, '제주도남부앞바다' AS PARENT_REGION, '연안바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S2320800' AS ZONE_CD, '경남중부남해앞바다중 연안바다' AS ZONE_NM, '경남중부남해앞바다' AS PARENT_REGION, '연안바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S2320900' AS ZONE_CD, '제주도동부앞바다중 북동연안바다' AS ZONE_NM, '제주도동부앞바다' AS PARENT_REGION, '북동연안바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S2321000' AS ZONE_CD, '제주도동부앞바다중 남동연안바다' AS ZONE_NM, '제주도동부앞바다' AS PARENT_REGION, '남동연안바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S2330100' AS ZONE_CD, '남해서부서쪽먼바다중 추자도연안바다' AS ZONE_NM, '남해서부서쪽먼바다' AS PARENT_REGION, '추자도연안바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S2330200' AS ZONE_CD, '제주도동부앞바다중 우도연안바다' AS ZONE_NM, '제주도동부앞바다' AS PARENT_REGION, '우도연안바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

MERGE INTO TB_ALERT_ZONE_MAP t
USING (SELECT 'S2330300' AS ZONE_CD, '제주도서부앞바다중 가파도연안바다' AS ZONE_NM, '제주도서부앞바다' AS PARENT_REGION, '가파도연안바다' AS DISTRICT_EXPR, 'csv_refresh' AS SOURCE_TYPE FROM DUAL) s
ON (t.ZONE_CD = s.ZONE_CD)
WHEN MATCHED THEN UPDATE SET
    t.ZONE_NM = s.ZONE_NM,
    t.PARENT_REGION = s.PARENT_REGION,
    t.DISTRICT_EXPR = s.DISTRICT_EXPR,
    t.SOURCE_TYPE = s.SOURCE_TYPE,
    t.USE_YN = 'Y'
WHEN NOT MATCHED THEN INSERT (ZONE_CD, ZONE_NM, PARENT_REGION, DISTRICT_EXPR, SOURCE_TYPE, USE_YN)
VALUES (s.ZONE_CD, s.ZONE_NM, s.PARENT_REGION, s.DISTRICT_EXPR, s.SOURCE_TYPE, 'Y');

